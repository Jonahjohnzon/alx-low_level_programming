#include "main.h"
/**
 * _islower - Check case
 * @c: Character to be checked
 * Description: 'Check for case difference'
 * Return: 1 or 2
 */
int _islower(int c)
{
	if (islower(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
