#include "main.h"
/**
 * _isalpha - Check case
 * @c: Character to be checked
 * Description: 'Check for case difference'
 * Return: 1 or 2
 */
int _isalpha(int c)
{
	if (_isalpha(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
